import React from 'react';

const PaymentForm = () => {
    return (
        <form className="payment-form">
            {/* Поля для формы оплаты */}
        </form>
    );
};

export default PaymentForm;
